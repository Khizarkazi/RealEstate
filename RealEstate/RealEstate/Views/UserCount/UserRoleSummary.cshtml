@* @model RealEstate.Models.UserRoleCountViewModel

<h2>User Role Summary</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Role</th>
            <th>Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Enum.GetValues(typeof(RealEstate.Models.UserRole)).Cast<RealEstate.Models.UserRole>())
        {
            <tr>
                <td>@item</td>
                <td>@Model.RoleCounts.GetValueOrDefault(item, 0)</td>
            </tr>
        }
    </tbody>
</table>
<a asp-controller="PropertyReport" asp-action="PropertyReportview" class="btn btn-primary" property=" Report">Property Report</a> *@


@model RealEstate.Models.UserRoleCountViewModel

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-4">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">
                <i class="bi bi-person-lines-fill"></i> User Role Summary
            </h4>
        </div>
        <div class="card-body">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Role</th>
                        <th class="text-center">Count</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Enum.GetValues(typeof(RealEstate.Models.UserRole)).Cast<RealEstate.Models.UserRole>())
                    {
                        <tr>
                            <td>@item</td>
                            <td class="text-center">@Model.RoleCounts.GetValueOrDefault(item, 0)</td>
                        </tr>
                    }
                </tbody>
            </table>

            <div class="mt-4 text-end">
                <a asp-controller="PropertyReport" asp-action="PropertyReportview" class="btn btn-outline-primary">
                    <i class="bi bi-house-door-fill"></i> View Property Report
                </a>
            </div>
        </div>
    </div>
</div>
